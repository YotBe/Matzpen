// Until real patient onboarding lands, every logged-in caregiver shares this
// demo patient document so the dashboard, alerts, and bureaucracy modules have
// something to read/write against.
export const MOCK_PATIENT_ID = 'demo-patient';
export const MOCK_PATIENT_NAME = 'דורון';
